import { ArrowRight, Code, Zap, GitBranch, Calendar } from "lucide-react";

export default function DashboardHome() {
  return (
    <div className="space-y-8">
      {/* 
      // TODO: DASHBOARD HOME - COMMENTED OUT FOR NOW
      // This page will show:
      // - Welcome message
      // - Quick stats (Active Projects, Deployments, Repositories, Last Deployed)
      // - Recent Projects list
      // - Quick actions and shortcuts
      // Currently redirecting to /dashboard/projects as the default view
      */}

      <div>
        <h1 className="text-3xl font-bold text-black dark:text-white mb-2">
          Welcome back
        </h1>
        <p className="text-gray-600 dark:text-gray-400">
          Dashboard home page - coming soon
        </p>
      </div>
    </div>
  );
}
